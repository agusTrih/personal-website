import Stat from '@/components/organisms/Stat';
import { FC } from 'react';

interface repoProps {}

const repo: FC<repoProps> = () => {
  return (
    <div>
      <Stat />
    </div>
  );
};

export default repo;
